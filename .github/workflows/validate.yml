name: Validate

on:
  push:
    branches:
      - main
      - develop
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    name: Validate custom component
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Validate Python syntax
        run: |
          python -m py_compile custom_components/smart_fan_controller/*.py

      - name: Validate JSON files
        run: |
          python -m json.tool custom_components/smart_fan_controller/manifest.json
          python -m json.tool custom_components/smart_fan_controller/translations/en.json
          python -m json.tool custom_components/smart_fan_controller/translations/fr.json

      - name: Check required files
        run: |
          test -f custom_components/smart_fan_controller/__init__.py
          test -f custom_components/smart_fan_controller/manifest.json
          test -f custom_components/smart_fan_controller/config_flow.py
          test -f custom_components/smart_fan_controller/const.py
          test -f README.md

      - name: Run validation script
        run: |
          chmod +x validate.sh
          ./validate.sh
